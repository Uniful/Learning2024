#include <unistd.h>

void ft_print_combn(int n)
{
	int i = n;

	int combinations[i];

	if (n <= 0 || n >= 10)
		return;
	while (i < 9)
		j = i + 1 while (j < 9)

	// 01, 02, 03, ..., 09, 12, ..., 79, 89
}

int main()
{
	ft_print_combn(int n);
	return (0);
}
